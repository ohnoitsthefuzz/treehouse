var answer = prompt('What programming language is the name of a gem?');
if ( answer.toUpperCase() === 'RUBY') {
  document.write("That's right!");
} else {
  document.write("<p>Sorry, that's not right...idiot.");
}
